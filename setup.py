from setuptools import setup, find_packages

setup(
    name='modif',
    version='0.0.0',
    author='Arish Khan',
    author_email='arish@sparkosol.com',
    description='All utility methods for rna modification prediction',
    packages=find_packages(),
    install_requires=[
    ],
    extras_require={
    },
    classifiers=[],
)
